name: Daily

on:
  workflow_dispatch:

permissions: {}

jobs:
  release:
    permissions:
      actions: read
      contents: write
      deployments: write
      packages: write
    uses: ./.github/workflows/release.yml
    with:
      force: true
      github_packages: false
      maven_central: false
    secrets: inherit
